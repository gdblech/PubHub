<resources>
    <!-- true to do backend testing locally, false to use production elements -->
    <bool name="backend">false</bool>
    <string name="testingBackend">localhost</string>

    <!-- set true to use the app camera, false to use enter the code. -->
    <bool name="camera">true</bool>

    <!-- set true if https if usable-->

    <!-- set true if you need to be able to use the skip to work button-->
    <bool name="development">true</bool>

</resources>